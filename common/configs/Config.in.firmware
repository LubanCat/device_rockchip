#

menu "Firmware"

config RK_PARAMETER
	string "parameter"
	default "parameter-ab.txt" if RK_AB_UPDATE
	default "parameter.txt"
	help
	  Name of parameter file under <CHIP_DIR>/.

if RK_KERNEL_CFG != ""

config RK_MISC_IMG
	string "misc image"
	default "blank"
	help
	  Name of misc image under <RK_IMAGE_DIR>/misc.

config RK_USE_FIT_IMG
	bool "use FIT (Flattened Image Tree) image"

endif

endmenu # Firmware
